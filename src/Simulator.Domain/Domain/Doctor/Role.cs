namespace Simulator.Domain.Doctor
{
    //Could be an abstract class if Role entity has logic based on the different role types
    public enum Role
    {
        Oncologist=1,
        GeneralPractitioner=2
    }
}